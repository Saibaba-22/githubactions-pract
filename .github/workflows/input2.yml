name: input field param
on: 
  workflow_dispatch: 
    inputs:
      environment: choose the environment to deploy 
      required: true 
      default: dev 
jobs:
  inputparam:
    runs-on: ubuntu-latest
    steps:
      - name: print the variable 
        run: echo " this is the job executed on ${{inputs.environment}}
